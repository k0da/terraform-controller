name: Test

on: pull_request

jobs:
  golangci:
    runs-on: ubuntu-latest
    name: lint
    steps:
      - name: checkout
        uses: actions/checkout@v1
      - name: golang-ci
        uses: golangci/golangci-lint-action@v2
        with:
          version: v1.29
